export const STAGE_PRODUCT_SUMMARY =
  "风格化 2.5D 场景语义图工具：绘制 / 程序化生成 SemanticControl，再生成 BaseColor（语义约束）提示词，供 Image2 出图。";

export const SEMANTIC_CONTROL_HINT =
  "SemanticControl 是本工具的核心产物：画好后即可生成 BaseColor 提示词喂给 Image2。BaseColor 回传后可作为参考叠加层迭代；Height 等由 TextureWiz 在外部生成。";

export const STAGE_NEW_HINT =
  "新建 Stage 将创建标准目录与 stage.json v2（仅 SemanticControl + BaseColor 槽位）。";
